# 交付验证记录

验证日期：2026-09-18。

## 构建与数据

- `npm run build` 成功，产物位于 `dist/`。
- `npm test`：7 项通过，0 项失败。覆盖三帧汇总、跳转后告警清除、不修改原数据、阈值边界、末帧停止、非法索引、子对象查找建筑编号、报告内容等行为。
- `npm run model` 导出真实 glTF 2.0 二进制模型，约 2.55 MB。来源为本项目程序化几何脚本。
- Blender 工具脚本已通过 Python 语法编译检查；未在 Blender 中实际运行，不能据此宣称完成了 Blender 往返验证。

## 浏览器检查

Playwright + 本机 Chrome，桌面 1536×960，手机 390×844。测试采用软件渲染，不能用于声明硬件帧率表现。

15 项完整流程检查通过，0 个未捕获页面异常：

1. GLB 在真实 WebGL 画布中加载，六栋建筑可识别。
2. 六栋建筑列表选择均更新对应详情。
3. 名称和编号搜索、空结果状态及清除搜索可用。
4. 09:05 同步更新功率、温度、告警数量、楼体标签及告警列表。
5. 09:10 与跳回 09:00 均清除过期告警。
6. 功率观察按当前数值排序。
7. 名称、绿化和阴影图层可切换。
8. 俯视切换可用，直接点击模型屋顶与点击标签都能选中对应建筑。
9. Markdown 快照、JSON 数据、GLB 模型和 PNG 场景截图均可下载。
10. 模拟播放推进并在末帧停止；重播和暂停有效。
11. 手机页面无横向溢出，楼宇选择保持可用。
12. 模型读取失败显示重试；恢复文件后能重新加载，期间数据控制仍可用。
13. WebGL 不可用时显示说明，模拟数据切换仍有效。
14. 中等密度显示 20 名学生和 4 名老师，人物位置随时间变化；低、中、高三档人数分别为 10、24、42。
15. 关闭人物图层后人物隐藏；系统启用“减少动态效果”时人物保持显示但停止移动。

## 视觉检查

已人工查看浏览器生成的桌面及手机长截图，检查模型、楼宇标签、三栏信息和手机纵向布局。已调整全景镜头以适配窄屏，并修正趋势图左侧刻度与数值重叠。

- `preview-desktop.png`：正常状态的桌面全景。
- `preview-alarm.png`：模拟告警状态。
- `preview-mobile.png`：手机纵向布局。
- `browser-results.json`：浏览器检查清单与实际执行时间。

## 已知限制

Three.js 引擎压缩后的独立脚本约 656 kB，超过 Vite 默认建议阈值，因此存在体积提示；构建成功。没有真实校园或设备连接，没有后台账号系统，所有数据明确为模拟。没有调用 Seed 模型；本成果不可作为 Seed 独立生成或其升级表现的实测证据。
